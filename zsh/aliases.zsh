alias reload!='. ~/.zshrc'

alias cls='clear' # Good 'ol Clear Screen command
alias box='cd ~/box'
alias df='cd ~/.dotfiles'

# when my terminal slows down, clear the logs'
alias clear_logs='sudo rm -rf /private/var/log/asl/*.asl'
